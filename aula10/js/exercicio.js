// Peça a idade e mostre um alert informando se o usuário é maior ou menor de idade.

const idade = prompt('Digite a idade do usuário:');

if (idade >= 18) {
    alert('Usuário maior de idade!')
} else {
    alert('Usuário menor de idade!')
}